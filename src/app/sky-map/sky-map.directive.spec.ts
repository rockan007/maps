import { SkyMapDirective } from './sky-map.directive';

describe('SkyMapDirective', () => {
  it('should create an instance', () => {
    const directive = new SkyMapDirective();
    expect(directive).toBeTruthy();
  });
});
